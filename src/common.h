#ifndef COMMON_H
#define COMMON_H

#include <stdio.h>
#include <stdlib.h>

// Function declarations
char* read_file(const char* filename);
void remove_html_extension(char *filename); 

#endif // COMMON_H